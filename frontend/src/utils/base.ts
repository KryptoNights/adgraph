import axios from "axios";
export const convertImgToBase64 = (imgStr: any) => {
  const axios = require("axios");
  console.log("apiDebug", JSON.stringify(imgStr));

  return JSON.stringify(imgStr);
};
